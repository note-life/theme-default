.loading-wrapper{
    padding: .6rem 0;
    text-align: center;
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: .42rem;
    color: #bacedd;
    font-weight: 600;

    img{
        width: .8rem;
    }
}